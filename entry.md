# Slermes Entry (v471)

## Quick Reference
- **Version:** v471 — Enhanced Slermes GUI to Hermes parity
- **Build:** `make desktop` — zero errors, zero warnings.
- **Ported:** 8,688/8,688 (100%) — all Python functions have C equivalents
- **Scanner:** slermes_parity_battleground.py — 100% PORTED
- **Desktop:** Enhanced ncurses GUI with sidebar, 8-tab settings, command palette, model picker, theme switching, session create/delete/rename, full statusbar
- **Desktop features implemented:** sidebar (sessions+nav), settings (8 tabs), command palette (25 cmds, live search), model picker, theme (dark/light/system), statusbar (gateway/tokens/tasks/YOLO), session ops (create/delete/rename), notification stack, role-labeled chat, composer with model pill

## What is Slermes?
A complete, standalone C reimplementation of Hermes Agent. Not a wrapper — a full behavioral-parity port.
Download the C binary, run it. No Python. No pip. No venv.
